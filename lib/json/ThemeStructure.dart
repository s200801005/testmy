// This code is translated from Java to Dart

class ThemeStructure {
  String name;
  String pathWord; // Using camelCase for Dart naming conventions
  int count;

  ThemeStructure({required this.name, required this.pathWord, required this.count});
}