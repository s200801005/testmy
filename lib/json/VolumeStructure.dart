// Framework: Dart

import 'ChapterStructure.dart';
import 'InfoBase.dart';
import 'ReturnBase.dart';

class VolumeStructure extends ReturnBase {
  Results results;

  // <ai>Preserve original comments if possible.</ai>
}

class Results extends InfoBase {
  List<ChapterStructure> list;

  // <ai>Preserve original comments if possible.</ai>
}